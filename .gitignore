# Secrets
.env

# Local runtime config (points at a provider endpoint; use config.example.yaml as template)
config.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# SDD scratch workspace (git-ignored per skill)
.superpowers/

# Git worktrees (git-ignored per skill)
.worktrees/
lab_tasks/
lab_runs/
